import 'package:flutter/material.dart';

// Extend the StatelessWidget to create our own Homepage widget
class CalcHome extends StatelessWidget{

  @override
  Widget build(BuildContext context){

    // A 'material' is like a canvas where we can put things on
    return new Material(
      color: Colors.blue,
    );
  }
}